# hsks-hanzi · 出片排期表（系统化覆盖）

> 守 [[GANGLING]]。取字完全确定：按 `02_hanzi.csv` 行序（认读优先），覆盖账本 `_coverage.json` 推进。

## 参数（定稿）
- **每条 18 字** = 3 页 × 6 字（2×3）。
- **命名**：`hsks-hanzi-<level>-NN`。出处角标 `HSK{级}·汉字·{认读/手写}`。
- **取字范围**：第 N 条覆盖行序 `[(N-1)×18 + 1, N×18]`。

## 各级出片量（每条 18 字）
| 级别 | 字数 | 视频数(约) |
|---|---|---|
| HSK1 | 246 | 14 |
| HSK2 | 225 | 13 |
| HSK3 | 434 | 25 |
| HSK4 | 591 | 33 |
| HSK5 | 581 | 33 |
| HSK6 | 563 | 32 |
| HSK7-9 | 1648 | 92 |

> 真源=`_coverage.json`。**依赖**：每字需补 `components.json`（部件）+ `vi-lexicon.json`（字义），缺则 build 报错列出。

## 生产顺序：级别升序，HSK1 全 → HSK2……。幂等复用区间。
