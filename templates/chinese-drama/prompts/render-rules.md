<!-- 出图铁律(用户锁定 2026-07-15)。每次真的出图(build/手动)前必读;写 script.json 每拍 shot.content 时逐条落实。 -->
# chinese-drama 出图铁律（真人质感·连续剧）

> 画风=**电影级真人质感 cinematic photorealistic**（旧版 webtoon 作废）。以下为强制约束，违反即废图。

## 1. 身高与身材（除老年人外，全员适用）
- **男性**：身高 **1.80m 以上**，身材高挑、肩宽、精瘦结实（lean athletic）。
- **女性**：身高 **1.70m 以上**，身材**非常苗条纤细、腿长**（tall slim slender long-legged）。
- **老年人豁免**：顾老夫人等长辈不受身高/身材/服装规则约束，按角色年龄自然呈现。
- 身高/身材是**不变量**，写进每个角色 `canonical.md`，每拍复用。

## 2. 女性服装（默认显身材 · 场景优先 · 连戏）
- **默认原则**：女性默认穿**美丽、时尚、显身材**的服装（短裤 / 短裙 / 吊带 / 露肩 / 收腰连衣裙，得体不低俗，SFW）——这是**总体倾向**，用于没有特定要求的日常/出街/居家场景。
- **★场景优先（铁律）**：当**当集场景明确要求某套服装时，以场景为准，覆盖默认原则**。例：婚礼当晚=红嫁衣、守灵/奔丧=素服白衣、当值女佣=制服、正式晚宴=礼服、雨中/睡前各按情境。别把"显身材短裙"硬套到这些场景上。
- **★连戏（铁律）**：**同一场景 / 同一时间线内，服装必须前后一致**（如婚礼当晚从头到尾都是同一件红嫁衣，不许一拍嫁衣、下一拍换短裙）。"每集换装"指**集与集之间、或换了场景/时间**才换，不是一集内乱换。
- 落地：服装由**每拍 `shot.content`** 现写覆盖，`canonical.md` 只锁脸/发/身材、不锁死某套衣服；同一场景多拍要在 shot 里写"同前一套 XX 服装"保连戏。

## 3. 出图输入=参考人物图 + 当集服装 + 当集场景（三件套）
- 每次真的出图，除了喂**该角色定妆参考图**（锁脸），**必须**在 prompt 里同时写清：
  1. **当集这一套服装**（第2条，具体到款式/颜色）；
  2. **当集这一个场景**（地点/光线/氛围/机位）。
- 即：`定妆参考图(锁脸) + shot.content(当集服装+场景+动作)`。缺服装或缺场景的 shot 视为未写完。

## 4. 单角色出图——禁止一图两张正脸（flux 不支持多角色）
- **同一张图里，禁止出现 2 个角色的正脸。** flux/kontext 单参考图只能锁 1 张脸，两张正脸必崩。
- 需要两人同框时：**只让被喂参考图的主角色露正脸**，另一人用**背影 / 过肩 / 侧脸剪影 / 只出手/局部**，绝不给第二张正脸。
- **★不露脸的人也必须写全（铁律，用户 2026-07-15 锁）**：走 flux 时，画面里每个不露正脸的角色，shot.content 必须**逐项写清具体情况——衣着（款式/颜色/连戏）、背影姿态、发型发饰、体态**，一项都不能省。教训：E01 s4 只写了 "the bride seen from behind" 没写服装 → flux 画成白色吊带，违反红嫁衣连戏，重摇一次才修好。
- **多人正脸需求 → 调用 nano-banana-pro**（多参考图各锁一脸，$0.15/张，一条视频最多 1 次）。除此之外每场景**单人 flux 出图**为默认（$0.04）。
- **★单人特写防"脑补听众"（实战教训 2026-07-16）**：prompt 里出现 "speaking to/looking at someone (out of frame)" 这类措辞时，flux 极易自作主张把"某人"画进前景（E02 s3 / E04 s4 / E04 s5 连续三次翻车）。凡单人拍必须显式写 `completely ALONE in the frame, NO other person, NO foreground figure`，并避免 "对着某人说话" 措辞、改写成 "facing the camera / toward the door" 等无人称指向。

## 5. 真人质感·去塑料感
- 追求**真实照片质感**：自然肤质、细腻毛孔、真实光影、轻微胶片颗粒；**避免**塑料感/过度磨皮/CGI 感/油光蜡像（no plastic skin, no waxy CGI look, natural matte realistic skin）。
- flux 安全过滤易对真人黑图 → payload 必带 `safety_tolerance:"6"`（gen-image 已内置）。出图不满意**禁止擅自重试，先问用户**（成本铁律）。

## 6. 真实人景比例·透视（像真的用相机拍出来的）
- 人物在场景里的**大小、比例、透视、景深必须符合真实拍摄**：由**机位/焦距/人物与相机的距离**决定人物在画面中的占比，不是把人像"贴"在背景上。
- 明确写机位与景别：远景(全身+环境)/中景(腰以上)/近景(胸像)/特写(脸)；人与场景处在**同一透视与光照**下（同一光源方向、同一色温、地面/家具与人脚的接触真实）。
- 全身/远景时脚要落地、比例正确；两人同框时近大远小符合站位（配合第4条：仅主角露正脸）。
- 一句话：**先想"这台相机站在哪、拍多远"，再让人物以真实比例长在那个场景里**。

## 7. 说话人标签不传给模型（TTS 只读正文）
- 脚本里「旁白：」「灵兰：」「顾晏辞：」这类**说话人标签**只是元数据（对应 script.json 的 `voice` 字段），**绝不送进 TTS 合成、也不上屏**——只有冒号后的正文台词才朗读/显示。
- 即：`voice` 决定谁在说、用什么音色；`captions` 只放正文。标签写进正文=会被念出来「旁白冒号…」，属错误。

## 8. 语言与音色（本剧铁律 · 音色表用户锁定 2026-07-15）
- **旁白 = 越南语**（受众母语，引导叙事）。
- **人物说话（中国角色）= 中文**（同时是学习内容，字幕三行拼音/中文/越南语，逐字卡拉OK落在中文行）。
- **灵兰内心思考 = 中文**（用户 2026-07-15 改，旧的"内心独白用越南语"作废）：音色=她**说话**的音色（`qingwu` 温柔小雅）；该拍 script.json 加 **`inner: true`**，字幕中文行前渲染 **💭 标记**，让观众知道这是心想、不是开口说。三行照常（拼音/中文卡拉OK/越南语）。
- **音色锁定表**（说话多的核心角色必须用用户指定音色，换音色须用户批准；均已免费试听验证）：

| voice 键 | 角色 | voice_type | 备注 |
|---|---|---|---|
| narrator | 旁白 | `vi_female_ruan_uranus_bigtts` | ★用户锁定 |
| linh | 灵兰·说出口的越南语（如情急脱口而出）/原主线 | `vi_female_partner_uranus_bigtts` | ★用户锁定 |
| qingwu | 灵兰·说中文 + **内心思考**（inner 拍同音色） | `zh_female_wenrouxiaoya_moon_bigtts` | ★温柔小雅，用户锁定 |
| yanci | 顾晏辞(男主) | `zh_male_aojiaobazong_moon_bigtts` | 傲娇霸总。旧的青苍被否：太老太成熟(2026-07-15) |
| xiaotao | 小桃 | `zh_female_roumeinvyou_emo_v2_mars_bigtts` | ★柔美女友，用户锁定 |
| liuwan | 柳梦烟(继母) | `zh_female_xinlingjitang_moon_bigtts` | ★心灵鸡汤(温柔假面贴"笑里藏针") |
| wanqing | 温晚晴(白月光) | `zh_female_zhixingnvsheng_mars_bigtts` | ★知性女生(海归)。⚠️ moon 后缀未开通(3001)，必须用 mars |
| yurou | 沈玉柔(白莲花) | `zh_female_lingling_uranus_bigtts` | ★玲玲姐姐，用户点名(2026-07-15 给 ID)。⚠️ 不返回字级时间戳→卡拉OK走均匀铺字 |
| chengye | 顾承业(二叔) | `zh_male_jingqiangkanye_moon_bigtts` | 自定义 |
| doctor | 医生(次要,无定妆图→全程不露正脸) | `zh_male_yuanboxiaoshu_moon_bigtts` | 渊博小叔(自定义,已试听) |
| laofuren | 顾老夫人 | 待定(暂占位知性女生 mars) | 老夫人**不适用**上面这批年轻女声；出场前试听老年感音色再锁 |

- 「玲玲姐姐」=`zh_female_lingling_uranus_bigtts`（用户 2026-07-15 给的 ID，已验证可调）→ 分给沈玉柔。点名音色样音统一放 `public/library/audio/voices/<中文名>__<voice_type>.mp3`。
- 新音色必须先**免费试听**（3001=未开通，不是 ID 写错也调不通）再写进 template.json（见 [[../../../public/library/audio/voices/README]]，样品在 `_voicetest/`）。
- **★音量统一（铁律，用户 2026-07-15 锁）**：不同音色出厂响度差很大（实测中文≈-15~-18 LUFS、越南语旁白≈-21~-23，差 5~7dB）。`scripts/tts.mjs` 合成后自动把每拍**纯增益归一到 -18 LUFS**（所有音色不削波可达的最大公共响度，实测收敛到 ±0.05dB），真峰值 -0.1dBTP 封顶。全剧所有音色一个响度，不许旁白小声、对白炸耳。

## 9. 字幕：全程三行·全程卡拉OK双语（用户 2026-07-15 锁）
- **每一拍都是三行**：`拼音 / 中文(毛笔行楷 Ma Shan Zheng) / 越南语`。**没有只越南语的单行拍**——旁白、独白、对白**一律带中文行**。
- **★中文↔越南语一一对应（铁律）**：越南语拍的中文行=该句越南语的**完整对应翻译**（意思一一对应，不是短总结/概括）。**严禁"几个中文字对着一大段越南语"**。
- **★一行放不下就拆句顺序显示（铁律，用户 2026-07-15 定）**：一句在一行放不下（中文约 >10 字），就把它**拆成 2+ 个顺序拍**（同一 `sceneId` 共用同一张图），每拍一小句、短到一行放得下、中文↔越南语逐句对应，**这句播完接下句**。**不是换行、不是滚动条、不是缩小字号**——是分句次第出现。拆句在 `script.json` 里做（每小句一个 beat，voice/sceneId 不变，首拍带 shots）。
- **每一拍中文行都走逐字卡拉OK**（逐字弹起+点亮青色）：
  - 中文对白拍：用火山中文 TTS 的**真字级时间戳**驱动。
  - 越南语拍(旁白)：中文行没有真时间戳 → `build.mjs` 用 `evenCharTimings()` **按本拍音频时长把汉字均匀铺开**驱动卡拉OK，扫字跟着越南语音频走。（内心思考拍现在是中文拍，走真时间戳。）
- **★越南语行也必须卡拉OK（铁律，用户 2026-07-15 锁）**：逐词弹起+点亮，与中文行同一套视觉语言。越南语音色不返回词级时间戳 → `build.mjs` 用 `evenWordTimings()` 按本拍音频时长**逐词均匀铺开**驱动（manifest `viWordTimings`）。三行里不许有"死"的静态行。
- **★三行一律禁止换行（铁律，用户 2026-07-15 锁）**：拼音/中文/越南语每一行都必须**单行放下，越南语也不许换行**。一行放不下 → 拆拍（同 sceneId 顺序拍）。渲染层 FitLine 单行等比缩小只是兜底，**不许依赖它救长句**（缩得明显变小=废稿）。
- **★行尾禁止标点（铁律，用户 2026-07-15 锁）**：三行每一行的**结尾不得出现任何标点符号**（，。！？…—、"" ,.!?;: 等）。句中标点允许（如「医生一来，就会发现」）。写 captions 时直接去掉尾标点再入库。
- 字体（用户 2026-07-15 锁）：中文=`library/fonts/MaShanZheng.ttf`(毛笔行楷)；**拼音行+越南语行统一=Itim**（手写感但收敛、不死板——Charm 弯钩体被否：太夸张），Nunito 兜底。换手写字体必须先 fontTools 验字形覆盖（缺越南语附加符或拼音 ǎǐǒǔǖǘǚǜ 的不能用）。版面照 chinese-learn(黑底/上图720横屏/下三行字幕)。
- **卡拉OK动效分工（用户 2026-07-15 锁）**：只有**中文行逐字弹跳**；拼音行、越南语行**只变色不跳动**。**拼音颜色与汉字完全同色同步**（当前=青色/已读=白/未读=灰，不再单独的拼音色）；越南语行=已读金/当前青/未读灰。
- 实现落点：`build.mjs`(按 vi_/zh_ 判语言合成 + evenCharTimings 补时间戳) + `src/layouts/chinese-drama.tsx`(三行渲染,中文行 KaraokeRow,越南语行换行块)。

## 10. 海报式封面（每集一张，用户 2026-07-15 锁：封面=海报，同一张图）

- **每次生成完一集视频，必须交付一张海报式封面**，尺寸=视频同款 **1080×1440**：
  - **底图**：fal **单独新出**一张 3:4 竖版图（计入 cost 日志）——画面**不得与视频内任何画面重复**，但必须**紧扣本集主题**（同人物/服装连戏/同世界观的全新构图，遵守本文件 1–6 条出图铁律）。
  - **排版必须按电影海报设计，禁止照搬字幕三行样式/配色**。固定构成（`src/CoverDrama.tsx`，composition `cover-drama`）：
    1. 左上：**竖排系列名**（铃兰穿越记）+ **红印章集数**（E01=全剧集号，每集递增，与卷标签不重复）；
    2. 右上：**卷标题**金线框标签（如「卷一 · 惊醒 / Quyển 1 · Tỉnh Giấc」，百集一换，到 E101 换卷二）；
    3. 主标题：**章主题名**（arc 十集一章的章名，金色渐变毛笔大字）；
    4. 红条：**当集钩子标题**（写本时现起，要勾人：悬念/利害/倒计时）；
    5. 底部：系列越南语名。
  - 章主题名与钩子标题都必须**拼音+中文+越南语三行齐全**；行尾禁标点；全部 FitLine 单行禁溢出；配色走海报金/红/奶白系，不用字幕的青/黄。
  - **字号比例（用户 2026-07-15 锁）**：拼音/越南语行**加大**、中文行**适度收小**（拼音≈中文的 0.45、越南语≈中文的 0.6，已固化在 CoverDrama.tsx），标题块整体占位不变。
- 渲染：`npx remotion still src/index.ts cover-drama <中文名>-封面.png --props=<视频目录>/cover.json`。
- 交付名（放视频目录）：`<中文名>-封面.png`（**只此一张**，没有单独的"海报"文件）。

## 关联
[[script-rules]] · [[../story/00-bible]] · [[../authoring.md]] · [[style]] · 各角色 [[../characters]]/*/canonical.md
