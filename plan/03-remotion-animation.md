# 03 · Remotion 绘本页动效与排版

渲染层是**纯数据驱动**：一个万能 Composition 读 `manifest.json` 渲染，自身不含业务内容。
`calculateMetadata` 读 manifest 累加各页 `durationMs` 得总时长与 fps。

## 一段旁白内的多图过程动画
一个 page 可含 `shots[]`（1~N 张）。按 `weight` 把该页 `durationMs` 分给各 shot，
shot 间用交叉淡入/顺序揭示表现"过程"（如山底→山中→山顶的攀爬）。单图时退化为静态页 + Ken Burns。

## "翻小人书"动感 = 这几样叠加（都不烧 API）
1. **翻页转场**：镜头间做翻书/卷页（CSS 3D `rotateY` 或卷曲）。"小人书"感的灵魂。
2. **Ken Burns**：单页内极缓慢推拉/平移，静图也有呼吸。
3. **Line boil（手绘抖动）**：每隔几帧让画面/线条极轻微位移+旋转，模仿逐帧手绘。便宜且出彩。
4. **纸张质感 + 暗角**：叠纸纹理 + vignette，强化绘本翻看感。
5. **视差（v2 可选）**：若 manifest 提供 `imageLayers{bg,character}`，角色与背景错位轻晃。成本翻倍，先不做。
6. **翻页音效 + 轻 BGM**：见 [[04-tts-captions]] 音频部分。

## ★★ 版式：1:1 方图合成到白底 9:16（2026-07-03 用户锁定，取代旧 bandTopRatio）
出图**统一 1:1 方图**（`settings.image.aspectRatio="1:1"`）；渲染器 `Video.tsx` 把方图放到**白底 9:16** 画布上，按 **16 等分**固定排布：

```
┌────────────────────┐  0
│  顶部留白 ≈0.67/16  │  80px         ← 纯白极窄；整体上移（用户 2026-07-03 二次确认）
├────────────────────┤  80
│                    │
│    1:1 方形画面     │  1080px (9/16) ← 满宽正方，AI 只管画这张方图
│                    │
├────────────────────┤  1160
│    字幕带 3/16      │  360px         ← 拼音/中文/越南语，逐行中越对照，居中
├────────────────────┤  1520
│  底部留白 ≈3.33/16  │  400px         ← 纯白，多出的留白全放这里
└────────────────────┘  1920 (16/16)
```
- **为什么 1:1**：AI 只画一张普通方形插画，白边/字幕带/留白全由渲染器精确控制 → 版式永远统一，且**根治"底部留白被 AI 画成框"的问题**（见 [[10-art-style-locked]] 4.4 死命令）。
- **★ 整体上移（2026-07-03 用户锁定）**：顶部留白从 2/16(240px) **砍到 1/3 ≈ 80px**，省下的 160px **全部移到底部**（底留白 2/16→≈3.33/16=400px）。画面+字幕都以顶留白为锚点往下排，所以减顶留白 = 整块内容上移。数学：80+1080+360+400=1920 ✓。
- 渲染层实现：`imgTop = round(height*2/16/3)`，`capTop = imgTop + imgSize`，其余不变。
- 出图提示词**只描述方形画面本身**（居中主体 + 大片纯色纸背景），**绝不提**框/字幕/底部留白。
- **★ 尺寸归一化（2026-07-03）**：flux 出图人物大小每张不一，`build.mjs` 用 sharp 量白底上人物身高占比、算出 `imgScale` 缩到统一目标（`charTargetHeight` 0.66），渲染层把方图 `scale` 乘 `imgScale`，**所有拍人物一样大(=p6)**，四周露纯白画布(`bgColor #ffffff`)、无缝。见 [[10-art-style-locked]] 4.6/4.8。

## 竖屏构图与安全区（旧，已被上面的 1:1 合成取代）
- ~~画面主体居中偏上；底部留给三行字幕。~~ 现由渲染器合成控制。

## 字幕排版（拼音 + 中文 + 越南语）
```
jiǎ zhōu jí zhě, fēi néng shuǐ yě,     ← 拼音，声调符号 ā á ǎ à（字号 34）
假舟楫者，非能水也，                     ← 中文，主视觉（字号 67）
Người mượn thuyền bè, chẳng biết bơi,   ← 越南语意译（字号 50），紧贴其对应中文
ér jué jiāng hé.
而绝江河。
mà vượt được sông lớn.
```
- **★ 字体（2026-07-03 用户锁定）**：中文用 **SimHei 黑体**（粗、清晰，视频字幕更好认，取代旧 ZCOOL 快乐体）；拼音声调 / 越南语附加符号（ạ ầ ễ）用 **Nunito** 圆体覆盖。字体文件 `public/library/fonts/{SimHei.ttf,Nunito.ttf}`，`src/fonts.ts` 加载。
- **★ 字号取中值（2026-07-03）**：中文 **67** / 拼音 **34** / 越南语 **50**（在"看不清的小版"与"过大版"之间取中间，`RubyRow` 与越南语 span）。
- **★★ 逐行中越对照（2026-07-03 用户锁定，根因修复）**：越南文**必须直接排在它对应的那行中文正下方**，不能"所有中文行在上、所有越南文挤到最底下"。
  - 数据：`captions.lines = [{zh, vi}, ...]`，**每个元素 = 一行中文 + 对应越南语**，由分镜按语义切分（分句对齐，见 [[04-tts-captions]] / storyboard）。没有 `lines` 时退化为整句中文+整句越南语一对。
  - 渲染：`Video.tsx` 按 `pairs` 渲染，一对内（拼音+中文）与越南语**紧贴**（gap 4），对与对之间留稍大间距（gap 14），整组在字幕带内**垂直居中**。
  - `build.mjs` 透传 `beat.captions.lines` 进 manifest。TTS 仍用整句 `captions.zh`，拼音渲染层自动注音。
- **单行不折行自适配**：每行 `white-space: nowrap`，`FitLine` 用 `useLayoutEffect` 量 `scrollWidth`、算 `scale=min(1,avail/measured)` 整行等比缩小贴合（配 `delayRender/continueRender` 保渲染确定性）。
- 停留时长 = 该页 TTS 真实音频长度（音频驱动）。
- **逐字高亮（卡拉OK跟读）**：若火山 TTS 提供字级时间戳（见 04），中文逐字点亮；否则整句淡入淡出。

## ★ 运镜升级：组合运镜（已锁定，2026-07-02）
`config/motion.json` 预设支持组合字段：`scale`(推拉) + `panX/panY`(平移) + `driftX/driftY`(正弦漂移，让静图持续"呼吸") + `rotate`(轻微旋转) + `ease`(inOut 缓动)。
- 预设：`push-in / pull-back / pan-left / pan-right / climb-up / ken-burns / sway / pop / still`。
- 分镜按句意（`rules.byKeyword`）自动选 preset；渲染层 `Video.tsx` 按名播放，**改预设即调运镜，不动代码**。
- beat 间用 `@remotion/transitions`（淡入/滑入/翻页）做转场，不再只是 opacity 淡入淡出。

### ★ 多样化运镜铁律（2026-07-03 用户强调）
- **别全片一个运镜**：按每拍**实际场景/内容**选不同预设，让镜头有变化。
  - 朗读金句/郑重 → `push-in`；恍然大悟/情绪高点 → `pop`；讲道理铺陈 → `pan-left/right`；
    「越来越好/领悟登高」→ `climb-up`；平静空镜 → `ken-burns/sway`；收尾定格 → `still`。
- **相邻两拍尽量不用同一个预设**，避免观感呆板。`script.json` 逐拍显式写 `motion`（不确定再落到 `rules.byKeyword` 自动选）。
- 运镜是"没特效也好看"的主力（特效已精简到 ≤2/片，见 [[11-effects-spec]]），所以运镜的多样性更重要。

## 片头 / 片尾
- 片头：品牌露出 + 主题。
- 片尾：软件 CTA「下载 XX，全家一起学中文」+ 二维码/应用商店标识。固定模板。

## 关联
[[00-overview]] · [[01-pipeline]] · [[04-tts-captions]]
